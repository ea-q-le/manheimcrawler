package tables;

public class VehiclesTable {
	
	//TODO -> define variables and methods for the following table ddl
	// table is already created in the server
	/*	create table vehicles (

			id bigint auto_increment primary key not null,
		    year int null,
		    make_model varchar(128) null,
		    vin varchar(32) null,
		    odometer int null,
		    acution varchar(32) null,
		    lane varchar(16) null,
		    run_date datetime null,
		    announcements varchar(256) null,
		    available bool not null

		);
	*/


}
